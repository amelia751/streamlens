# Local Cursor rules (key paths, machine-specific)
.cursor/rules/

# Credentials — never commit
secrets/
**/*-sa.json
**/gcp-owner.json
**/gcp*.json

# Isolated gcloud CLI state (do not use ~/.config/gcloud)
.gcloud/

# Saved conversations (local SQLite session store)
backend/.data/

# Downloaded dumps — scripts and READMEs stay tracked
data/**/*.xlsx
data/**/*.xls
data/**/*.tsv
data/**/*.tsv.gz
data/**/*.csv
data/**/*.zip
data/**/*.gz
data/**/*.json
data/**/_tmp*/

.DS_Store
.env
.env.*

# Hackathon writeup (local only)
submission/

# App
node_modules/
.next/
*.tsbuildinfo
.venv/
__pycache__/
*.pyc
scripts/youtube/.crawl-cache.json
